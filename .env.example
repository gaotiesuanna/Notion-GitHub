# Notion 配置
NOTION_TOKEN=your_notion_integration_token_here
NOTION_PROJECTS_DATABASE_ID=your_projects_database_id_here
NOTION_PAPERS_DATABASE_ID=your_papers_database_id_here
# 兼容旧配置（可选，未拆分时可继续使用）
NOTION_DATABASE_ID=your_legacy_database_id_here

# GitHub 配置 (可选,用于提高 API 限制)
GITHUB_TOKEN=your_github_token_here

# 项目配置文件 (推荐 .xlsx)
PROJECTS_FILE=data/projects.xlsx

# 同步模式:
# all         -> 新建 + 更新 (默认)
# create_only -> 只新建,已存在 notion_page_id 的项目不更新
# update_only -> 只更新,无 notion_page_id 的项目不新建
SYNC_MODE=all

# 论文同步时是否强制用 arXiv 标题覆盖现有标题
# true  -> 覆盖
# false -> 仅在标题为空时补齐 (默认)
FORCE_ARXIV_TITLE=false

# 是否在主同步前,先按 Notion 页面“分类”字段反向对齐 data/projects.xlsx
# true  -> 启用 (会新增分类并移动项目)
# false -> 关闭 (默认)
SYNC_CATEGORY_FROM_NOTION=false


# 飞书需要创建应用，并且将应用添加到电子表格上去，还需要赋予应用一定的权限
# 具体参考飞书官方文档：https://open.feishu.cn/community/articles/7298446935350231044
# 关于详细的API，查阅官方文档：https://open.feishu.cn/document/server-docs/docs/sheets-v3/data-operation/prepend-data
LARK_APP_ID=your_lark_app_id
LARK_APP_SECRET=your_lark_app_secret
LARK_APP_TOKEN=your_lark_app_token
LARK_SHEET_TOKEN=your_lark_sheet_token
